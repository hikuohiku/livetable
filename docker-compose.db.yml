services:
  db:
    build: ./database
    ports:
      - "58923:58923"
    environment:
      TZ: "Asia/Tokyo"
    env_file:
      - .env.local
    restart: always
    volumes:
      - ./database/data:/var/lib/mysql
      - ./database/my.cnf:/etc/my.cnf
